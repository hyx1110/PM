# 三人项目详细分工与协作实施指南

## 1. 文档目的

本文档用于指导 3 人团队完成“项目任务与人力协同管理系统”的实际开发。

目标是明确：

- 三个人分别负责什么。
- 哪些事情必须一起完成。
- 每个人每天具体应该做什么。
- 前端、后端、数据库如何配合。
- 如何使用 Git 协作。
- 如何避免代码冲突。
- 如何进行接口联调。
- 如何测试。
- 如何完成部署。
- 如何推进一期开发。

建议团队角色：

```text
A：前端负责人
B：基础后端 / 架构 / 数据库负责人
C：业务后端负责人
```

---

# 2. 三个人总体分工

## A：前端负责人

主要技术：

- Vue 3
- TypeScript
- Vite
- Element Plus
- Tailwind CSS
- Pinia
- Vue Router
- Axios

主要负责：

- 所有前端页面。
- 页面布局。
- 公共组件。
- API 调用。
- 权限菜单。
- 共享看板 UI。
- 报表 UI。
- Dashboard。
- 前后端联调。
- 前端测试。

---

## B：基础后端 / 架构 / 数据库负责人

主要技术：

- Python
- FastAPI
- SQLAlchemy
- Pydantic
- Alembic
- MySQL
- JWT
- RBAC

主要负责：

- 后端项目骨架。
- 数据库设计。
- 登录。
- 用户。
- 组织。
- 角色。
- 权限。
- 项目。
- 项目成员。
- Alembic 数据库版本管理。
- API 基础规范。
- 权限中间件。
- 部署核心配置。

B 是整个项目后端技术基础负责人。

---

## C：业务后端负责人

主要技术：

- Python
- FastAPI
- SQLAlchemy
- Pydantic
- MySQL

主要负责：

- 任务。
- 排期。
- 人力预约。
- 预约确认。
- 人力冲突检测。
- 执行记录。
- 实际工时。
- 项目过程管理。
- 达成率。
- 达成质量。
- 风险。
- 工时统计。
- 共享看板业务 API。

C 是系统核心业务逻辑负责人。

---

# 3. 分工原则

不要采用：

```text
A：前端
B：后端
C：数据库
```

因为数据库无法作为完全独立岗位存在，会产生大量依赖。

推荐：

```text
A：页面与交互

B：基础能力与数据库

C：业务逻辑
```

三个角色关系：

```text
                  A 前端
                    │
                    │ API
                    ▼
        ┌─────────────────────┐
        │                     │
  B 基础后端              C 业务后端
        │                     │
        └─────────┬───────────┘
                  ▼
                MySQL
```

---

# 4. 项目启动前：三个人必须一起完成的事情

正式写代码前，三个人需要一起完成以下工作。

建议耗时：

> 1～2 天。

---

# 5. 第一步：统一业务理解

三个人一起阅读项目设计文档。

必须统一以下概念：

- 用户。
- 部门。
- 组织。
- 角色。
- 项目。
- 项目成员。
- 一级任务。
- 二级任务。
- 人力预约。
- 排期。
- 预计工时。
- 实际工时。
- 执行记录。
- 达成率。
- 达成质量。
- 风险。

三个人必须确认：

> 每个字段是什么意思。

否则后期最容易发生：

```text
A理解的“任务状态”
≠
B理解的“任务状态”
≠
C理解的“任务状态”
```

---

# 6. 第二步：确定一期功能范围

三个人一起确认 V1.0。

一期只实现：

1. 登录。
2. 用户。
3. 组织。
4. 角色权限。
5. 项目。
6. 项目成员。
7. 任务。
8. 共享看板。
9. 人力预约。
10. 预约确认。
11. 人力冲突检测。
12. 任务执行。
13. 项目过程报表。
14. 达成率。
15. 达成质量。
16. 操作日志。

一期暂缓：

- AI。
- MES/EES/YES。
- 消息通知。
- Redis。
- Kafka。
- K8S。
- 移动 APP。
- 复杂审批流。

---

# 7. 第三步：一起确定数据库

数据库设计由 B 牵头。

C 必须参与业务表设计。

A 需要了解字段，但不负责数据库。

建议第一期核心表：

```text
users
departments
organizations
roles
permissions
user_roles

projects
project_members

tasks

schedule_bookings

execution_records

task_evaluations

risk_records

operation_logs
```

---

# 8. B 负责数据库设计的具体步骤

## 第 1 步

创建 ER 草图。

例如：

```text
Department
   │
   ▼
 User
   │
   ├─────────────┐
   ▼             ▼
ProjectMember   ScheduleBooking
   │             │
   ▼             ▼
Project ─────── Task
                  │
                  ▼
          ExecutionRecord
                  │
                  ▼
          TaskEvaluation
```

---

## 第 2 步

确定每张表的字段。

例如 `users`：

```text
id
username
password_hash
name
email
department_id
status
created_at
updated_at
```

---

## 第 3 步

确定主键和外键。

例如：

```text
tasks.project_id → projects.id
tasks.owner_id → users.id

schedule_bookings.user_id → users.id
schedule_bookings.project_id → projects.id
schedule_bookings.task_id → tasks.id
```

---

## 第 4 步

确定唯一约束。

例如：

- username 唯一。
- role.code 唯一。
- project.code 唯一。

---

## 第 5 步

确定索引。

重点：

```text
schedule_bookings.user_id
schedule_bookings.start_time
schedule_bookings.end_time

tasks.project_id
tasks.owner_id

execution_records.task_id
```

---

## 第 6 步

使用 SQLAlchemy 创建 Model。

---

## 第 7 步

使用 Alembic 创建 migration。

例如：

```bash
alembic revision --autogenerate -m "create initial tables"
alembic upgrade head
```

---

## 第 8 步

将数据库设计提交给 C Review。

---

## 第 9 步

确认无误后合并。

---

# 9. 第四步：一起确定 API

B 和 C 负责定义接口。

A 必须参与确认。

原则：

> 接口先确定，再并行开发。

---

# 10. API 统一规范

统一前缀：

```text
/api/v1
```

统一成功返回：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

统一失败返回：

```json
{
  "code": 40001,
  "message": "invalid parameter",
  "data": null
}
```

分页：

```json
{
  "code": 0,
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```

---

# 11. 第五步：建立 Git 仓库

建议 Monorepo：

```text
project-management-system/
│
├── frontend/
│
├── backend/
│
├── docs/
│
├── deploy/
│
├── docker-compose.yml
├── README.md
└── .gitignore
```

---

# 12. Git 分支规范

主分支：

```text
main
develop
```

开发分支：

```text
feature/frontend-login

feature/frontend-project

feature/frontend-schedule

feature/backend-auth

feature/backend-project

feature/backend-task

feature/backend-schedule
```

Bug：

```text
fix/login-error
fix/schedule-conflict
```

---

# 13. Git 工作流程

每个人开始任务前：

```bash
git checkout develop
git pull
git checkout -b feature/xxx
```

开发完成：

```bash
git add .
git commit -m "feat: add project module"
git push origin feature/xxx
```

然后：

```text
创建 Pull Request / Merge Request
          ↓
其他成员 Review
          ↓
修改问题
          ↓
合并到 develop
```

禁止：

```text
直接 push main
```

---

# 14. Commit 规范

建议：

```text
feat: 新功能

fix: Bug修复

refactor: 重构

docs: 文档

style: 样式

test: 测试

chore: 工程配置
```

例子：

```text
feat: add schedule booking api

feat: add project list page

fix: resolve schedule time overlap

docs: update api document
```

---

# 15. 项目目录规范

# 15.1 前端

A 负责。

```text
frontend/
│
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── layouts/
│   ├── router/
│   ├── stores/
│   ├── types/
│   ├── utils/
│   ├── views/
│   └── App.vue
│
├── package.json
└── vite.config.ts
```

---

# 15.2 后端

B 负责基础骨架。

```text
backend/
│
├── app/
│   ├── main.py
│   │
│   ├── api/
│   │   └── v1/
│   │
│   ├── models/
│   │
│   ├── schemas/
│   │
│   ├── services/
│   │
│   ├── repositories/
│   │
│   ├── core/
│   │
│   └── utils/
│
├── alembic/
├── tests/
├── requirements.txt
└── Dockerfile
```

---

# 16. A：前端负责人详细任务

# 16.1 第一阶段：搭建前端项目

步骤：

1. 创建 Vue 3 + TypeScript 项目。
2. 配置 Vite。
3. 安装 Vue Router。
4. 安装 Pinia。
5. 安装 Axios。
6. 安装 Element Plus。
7. 安装 Day.js。
8. 配置 Tailwind CSS。
9. 配置开发代理 `/api`。
10. 配置 ESLint。
11. 创建目录结构。
12. 编写 README。

---

# 16.2 第二阶段：公共页面框架

A 创建：

```text
LoginLayout
MainLayout
Sidebar
Header
Breadcrumb
ContentArea
```

需要确定：

- 菜单样式。
- 页面宽度。
- 表格样式。
- 表单样式。
- 弹窗样式。
- 按钮规范。
- 状态标签规范。

---

# 16.3 第三阶段：Axios 封装

统一创建：

```text
src/api/request.ts
```

实现：

- Base URL。
- Token。
- 请求拦截。
- 响应拦截。
- 401 跳转登录。
- 统一错误提示。

---

# 16.4 第四阶段：登录页面

A 开发：

```text
/login
```

调用 B：

```text
POST /api/v1/auth/login
```

成功后：

- 保存 Token。
- 获取当前用户。
- 获取权限。
- 跳转 Dashboard。

---

# 16.5 第五阶段：用户管理页面

A 完成：

- 用户列表。
- 搜索。
- 新增。
- 编辑。
- 状态。
- 角色。

接口由 B 提供。

---

# 16.6 第六阶段：项目管理页面

A 完成：

```text
/projects
/projects/:id
```

功能：

- 项目列表。
- 新增项目。
- 编辑项目。
- 项目详情。
- 成员列表。
- 任务列表。

---

# 16.7 第七阶段：任务页面

A 负责：

- 任务列表。
- 新建任务。
- 编辑任务。
- 一级/二级任务展示。
- 负责人。
- 计划时间。
- 预计工时。

接口由 C 提供。

---

# 16.8 第八阶段：共享看板

这是 A 最重要的页面。

建议先做 V1：

```text
人员
×
日期
×
小时
```

步骤：

1. 先画静态表格。
2. 加人员数据。
3. 加日期。
4. 加小时。
5. 加排期色块。
6. 点击色块查看详情。
7. 点击空白创建预约。
8. 支持项目筛选。
9. 支持人员筛选。
10. 支持时间筛选。
11. 对冲突数据增加醒目标记。

一期不要一开始做复杂拖拽。

---

# 16.9 第九阶段：执行填报页面

A 开发：

- 我的任务。
- 任务详情。
- 实际开始。
- 实际结束。
- 实际工时。
- 执行说明。

---

# 16.10 第十阶段：过程报表

A 按文档设计表格：

```text
一级任务
二级任务
负责人
计划开始
计划结束
实际开始
实际结束
预估人力
实际人力
达成率
达成质量
```

---

# 17. B：基础后端负责人详细任务

# 17.1 第一阶段：后端项目骨架

B 创建：

```text
app/main.py
app/core/config.py
app/core/database.py
app/core/security.py
```

配置：

- FastAPI。
- CORS。
- 数据库。
- 环境变量。
- 日志。
- 异常处理。
- Swagger。

---

# 17.2 第二阶段：数据库连接

配置：

```text
SQLAlchemy
MySQL
Session
Engine
```

测试：

```text
GET /health
```

确认：

- 服务可启动。
- 数据库可连接。

---

# 17.3 第三阶段：认证

实现：

```text
POST /auth/login
GET /auth/me
```

实现：

- 密码校验。
- Token 生成。
- Token 解析。
- 当前用户。
- 登录保护。

---

# 17.4 第四阶段：用户

实现：

```text
GET /users
POST /users
GET /users/{id}
PUT /users/{id}
```

---

# 17.5 第五阶段：组织

实现：

- 部门。
- L1。
- L2。
- L3。
- L4。
- 主管关系。

---

# 17.6 第六阶段：RBAC

表：

```text
roles
permissions
user_roles
role_permissions
```

实现：

```text
当前用户
    ↓
角色
    ↓
权限
    ↓
API授权
```

---

# 17.7 第七阶段：项目管理

实现：

```text
GET /projects
POST /projects
GET /projects/{id}
PUT /projects/{id}
```

以及：

```text
POST /projects/{id}/members
DELETE /projects/{id}/members/{user_id}
```

---

# 17.8 第八阶段：操作日志

B 建立统一日志能力。

例如：

```python
log_operation(
    user=current_user,
    module="project",
    action="update",
    object_id=project.id
)
```

C 的业务接口也调用该能力。

---

# 18. C：业务后端负责人详细任务

# 18.1 第一阶段：任务模型

设计：

```text
tasks
```

字段：

```text
id
project_id
parent_id
name
task_type
owner_id
planned_start
planned_end
estimated_hours
status
description
```

实现 CRUD。

---

# 18.2 第二阶段：排期模型

设计：

```text
schedule_bookings
```

字段：

```text
id
user_id
project_id
task_id

start_time
end_time

planned_hours

status

created_by
created_at
updated_at
```

---

# 18.3 第三阶段：排期接口

实现：

```text
GET /schedules
POST /schedules
PUT /schedules/{id}
DELETE /schedules/{id}
```

---

# 18.4 第四阶段：冲突检测

创建预约之前检查：

```text
new_start < old_end
AND
new_end > old_start
```

SQL 思路：

```sql
SELECT *
FROM schedule_bookings
WHERE user_id = ?
  AND status IN ('pending','confirmed','running')
  AND start_time < :new_end
  AND end_time > :new_start;
```

存在记录：

```text
返回 Conflict
```

---

# 18.5 第五阶段：预约确认

实现：

```text
POST /schedules/{id}/confirm

POST /schedules/{id}/reject
```

状态：

```text
Pending → Confirmed
Pending → Rejected
```

---

# 18.6 第六阶段：执行记录

设计：

```text
execution_records
```

字段：

```text
id
task_id
user_id

actual_start
actual_end

actual_hours

status
description
exception_reason
```

---

# 18.7 第七阶段：过程管理报表

C 将：

```text
Task
+
Schedule
+
Execution
+
Evaluation
```

整合为报表数据。

---

# 18.8 第八阶段：达成率

一期简单处理：

- 后端提供字段。
- 管理人员填写。
- 后端保存。

后期再自动化。

---

# 18.9 第九阶段：达成质量

同样：

- 项目经理填写。
- 数据入库。
- 报表展示。

---

# 18.10 第十阶段：风险

一期先实现：

### 人力冲突

创建排期实时检测。

### 任务延期

```text
now > planned_end
AND
status != completed
```

### 人员过载

按天计算：

```text
sum(planned_hours) > standard_hours
```

---

# 19. A、B、C 如何并行工作

三个人不是串行开发。

应该：

```text
                API定义完成

       ┌──────────┼──────────┐
       │          │          │
       A          B          C
       │          │          │
     页面       基础API     业务API
       │          │          │
       └──────────┼──────────┘
                  ↓
                 联调
```

---

# 20. Mock 数据机制

B/C 接口还没完成时，A 不需要等待。

A 按接口文档模拟：

```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1,
        "name": "项目A"
      }
    ]
  }
}
```

因此：

```text
前端先开发
+
后端同时开发
```

---

# 21. API 文档责任

B 和 C：

> 写接口。

A：

> 验证接口是否满足页面。

Swagger：

```text
/docs
```

所有接口至少需要明确：

- URL。
- Method。
- 请求字段。
- 字段类型。
- 必填项。
- 返回字段。
- 错误码。

---

# 22. 每日协作机制

建议每天固定 10～15 分钟。

每个人回答：

1. 昨天完成什么。
2. 今天准备做什么。
3. 当前有什么阻塞。

例如：

```text
A：
共享看板基础页面完成。
今天接入排期列表接口。
阻塞：缺 schedule status 字段定义。

B：
用户登录完成。
今天做角色权限。
无阻塞。

C：
任务 CRUD 完成。
今天做排期冲突检测。
阻塞：需要确认预约状态。
```

---

# 23. 每日禁止做的事情

不要：

- 临时修改数据库不告诉别人。
- 临时修改字段名。
- 删除接口。
- 修改接口返回结构不通知 A。
- 直接 push main。
- 大量代码长期不提交。
- 在 develop 上直接开发。

---

# 24. 数据库修改规范

任何表结构变化：

```text
提出修改
   ↓
B/C讨论
   ↓
确认
   ↓
修改 SQLAlchemy Model
   ↓
生成 Alembic migration
   ↓
提交代码
   ↓
其他人 git pull
   ↓
alembic upgrade head
```

禁止：

```text
直接在 MySQL 手工 ALTER
然后不提交 migration
```

---

# 25. 接口变更规范

例如 C 原接口：

```json
{
  "user": "A"
}
```

不要直接改成：

```json
{
  "owner": "A"
}
```

正确做法：

1. 告知 A。
2. 更新 API 文档。
3. 修改后端。
4. 修改前端。
5. 联调。
6. 合并。

---

# 26. Code Review

至少一个人 Review 后才能合并。

A 的代码：

- B 或 C Review。

B 的代码：

- C Review。

C 的代码：

- B Review。

Review 内容：

- 是否影响已有功能。
- 是否重复代码。
- 是否有安全风险。
- 是否符合目录规范。
- 是否有明显性能问题。
- 接口是否符合约定。

---

# 27. 第一周开发计划

## A

- 创建 Vue 项目。
- 登录页。
- 主布局。
- 菜单。
- 用户页面。
- 项目页面骨架。

## B

- 创建 FastAPI 项目。
- MySQL 连接。
- Alembic。
- User。
- Auth。
- Role。
- Permission。

## C

- Task Model。
- Task CRUD。
- Schedule Model。
- Schedule CRUD。

## 周末目标

```text
用户可以登录
+
可以看到项目
+
可以创建任务
+
可以看到基础排期数据
```

---

# 28. 第二周开发计划

## A

- 共享看板。
- 项目成员页面。
- 任务管理页面。
- 预约弹窗。

## B

- 组织管理。
- 项目管理 API。
- 项目成员 API。
- RBAC 完善。

## C

- 人力冲突检测。
- 预约确认。
- 执行记录。
- 实际工时。

## 周末目标

```text
创建项目
    ↓
添加成员
    ↓
创建任务
    ↓
预约人员
    ↓
成员确认
```

完整跑通。

---

# 29. 第三周开发计划

## A

- 我的任务。
- 执行填报。
- 项目过程报表。
- 达成率/质量页面。

## B

- 操作日志。
- 权限完善。
- API异常处理。
- 数据初始化。

## C

- 过程报表接口。
- 达成率接口。
- 达成质量接口。
- 延期判断。
- 人员负载计算。

## 周末目标

完整闭环：

```text
计划
↓
预约
↓
执行
↓
填报
↓
报表
↓
评价
```

---

# 30. 第四周开发计划

三个人主要做：

- 联调。
- Bug。
- 权限检查。
- 数据校验。
- UI 优化。
- 部署。
- 用户测试。

---

# 31. 联调顺序

不要最后统一联调。

推荐边开发边联调。

顺序：

```text
登录
↓
用户
↓
项目
↓
项目成员
↓
任务
↓
排期
↓
预约确认
↓
执行
↓
报表
↓
风险
```

一个模块完成：

> 马上联调。

---

# 32. 测试分工

## A

负责前端：

- 页面。
- 表单。
- 路由。
- 权限显示。
- 错误提示。
- 浏览器兼容。

## B

负责：

- 登录。
- 权限。
- 用户。
- 项目。
- 数据库。
- API 安全。

## C

负责：

- 排期。
- 冲突。
- 工时。
- 报表。
- 风险。
- 时间边界。

---

# 33. 冲突检测重点测试用例

必须测试：

### Case 1

已有：

```text
09:00 - 12:00
```

新增：

```text
10:00 - 11:00
```

结果：

> 冲突。

### Case 2

已有：

```text
09:00 - 12:00
```

新增：

```text
12:00 - 14:00
```

结果：

> 不冲突。

### Case 3

已有：

```text
09:00 - 12:00
```

新增：

```text
08:00 - 10:00
```

结果：

> 冲突。

### Case 4

已有：

```text
09:00 - 12:00
```

新增：

```text
08:00 - 13:00
```

结果：

> 冲突。

---

# 34. 部署分工

B 主负责。

A/C 配合。

部署架构：

```text
Browser
   │
 Nginx
   │
 ┌─┴────────────┐
 │              │
Vue            /api
                │
             FastAPI
                │
              MySQL
```

---

# 35. 部署步骤

## 前端

A：

```bash
npm run build
```

生成：

```text
dist/
```

交给 Nginx。

---

## 后端

B：

创建生产配置。

启动：

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

正式环境可配 Gunicorn。

---

## 数据库

B：

```bash
alembic upgrade head
```

导入基础角色和管理员。

---

# 36. 环境配置

建议：

```text
.env
```

例如：

```env
DATABASE_HOST=
DATABASE_PORT=3306
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=

JWT_SECRET=
JWT_EXPIRE_MINUTES=
```

不要把密码直接提交 Git。

---

# 37. 项目文档责任

## A

维护：

```text
FRONTEND.md
```

## B

维护：

```text
DATABASE.md
AUTH.md
DEPLOY.md
```

## C

维护：

```text
BUSINESS_RULES.md
```

B/C 共同维护：

```text
API.md
```

---

# 38. Definition of Done

一个功能只有满足以下条件才算完成：

1. 代码完成。
2. 自测通过。
3. 接口文档更新。
4. 数据库 migration 已提交。
5. 前后端已联调。
6. 没有明显 Bug。
7. Code Review 通过。
8. 已合并 develop。

---

# 39. 项目问题处理流程

出现问题时：

```text
发现问题
   ↓
创建 Issue
   ↓
确认负责人
   ↓
复现
   ↓
分析
   ↓
修复
   ↓
测试
   ↓
Review
   ↓
合并
```

---

# 40. Issue 格式

建议：

```text
标题：
[Schedule] 同一人员可以重复预约

环境：
dev

复现步骤：
1.
2.
3.

预期：
应该提示冲突

实际：
预约成功

负责人：
C
```

---

# 41. 每周例会

建议每周一次 30～60 分钟。

内容：

1. 当前完成率。
2. 未完成模块。
3. Bug。
4. 新需求。
5. 是否影响一期范围。
6. 下周计划。

最重要原则：

> 新需求不要直接插入当前 Sprint。

先评估。

---

# 42. 三人协作最重要的边界

## A 不负责

- SQL。
- ORM。
- 数据库 migration。
- 后端业务判断。

## B 不负责

- 复杂业务排期逻辑。
- 大量前端页面。

## C 不负责

- 用户认证体系。
- 全局 UI。

但：

> 三个人都必须理解整个系统。

---

# 43. 最终责任矩阵

| 模块 | A 前端 | B 基础后端 | C 业务后端 |
|---|---|---|---|
| 登录 | 主责 | 主责 | 协助 |
| 用户 | 主责UI | 主责API |  |
| 组织 | 主责UI | 主责API |  |
| 权限 | 主责UI | 主责API |  |
| 项目 | 主责UI | 主责API | 协助 |
| 项目成员 | 主责UI | 主责API | 协助 |
| 任务 | 主责UI | 协助 | 主责API |
| 共享看板 | 主责UI | 协助 | 主责API |
| 人力预约 | 主责UI |  | 主责API |
| 预约确认 | 主责UI |  | 主责API |
| 冲突检测 | 展示 |  | 主责 |
| 执行填报 | 主责UI |  | 主责API |
| 过程报表 | 主责UI | 协助 | 主责API |
| 达成率 | 主责UI |  | 主责API |
| 达成质量 | 主责UI |  | 主责API |
| 风险 | 主责UI | 协助 | 主责API |
| 操作日志 | 主责UI | 主责基础 | 调用 |
| 数据库 | 了解 | 主责 | 业务表协助 |
| API规范 | 参与 | 主责 | 主责 |
| 部署 | 前端配合 | 主责 | 后端配合 |
| 测试 | 前端 | 基础功能 | 业务功能 |

---

# 44. 最推荐的真实开发顺序

```text
Day 1
需求确认

Day 2
数据库 + API

Day 3
项目骨架

Day 4~5
登录 / 用户 / Task

Week 2
项目 / 成员 / 共享看板 / Schedule

Week 3
预约确认 / 执行 / 报表

Week 4
风险 / 测试 / Bug / 部署
```

---

# 45. 最核心协作原则

三个人始终遵守：

> **接口先行、模块拆分、独立分支、小步提交、及时联调、数据库统一管理。**

如果这 6 条能够严格执行，3 人团队完全可以稳定完成第一版管理系统。
