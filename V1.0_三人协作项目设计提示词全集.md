# 项目任务与人力协同管理系统 V1.0
# 三人协作项目设计提示词全集

> 本文档用于指导 3 人团队基于 AI 辅助完成 V1.0 版本开发。  
> 适用技术栈：Vue 3 + TypeScript + Vite + Element Plus + Pinia + Axios + FastAPI + SQLAlchemy + Alembic + MySQL 8 + JWT + RBAC。  
> 建议使用方式：每完成一个任务，就将对应“提示词”单独发送给 AI，让 AI 按指定目录、接口、约束和验收标准生成代码或设计结果。

---

# 一、V1.0 总体目标

V1.0 只实现系统最核心闭环：

```text
登录
  ↓
用户 / 组织 / 权限
  ↓
项目
  ↓
项目成员
  ↓
任务
  ↓
共享看板
  ↓
人力预约
  ↓
预约确认
  ↓
冲突检测
  ↓
任务执行
  ↓
过程报表
  ↓
达成率 / 达成质量
  ↓
操作日志
```

V1.0 暂不实现：

- AI 自动排期
- AI 风险预测
- MES/EES/YES 实时集成
- Redis
- Kafka
- Elasticsearch
- Kubernetes
- 移动 APP
- 复杂审批流
- 消息中心
- 拖拽式高级排期
- Excel 导入导出

---

# 二、统一项目约束

以下内容建议作为所有提示词的公共前置约束。

## 2.1 前端技术栈

```text
Vue 3
TypeScript
Vite
Vue Router
Pinia
Axios
Element Plus
Tailwind CSS
Day.js
```

## 2.2 后端技术栈

```text
Python 3.11+
FastAPI
Pydantic
SQLAlchemy
Alembic
MySQL 8
JWT
RBAC
Pytest
```

## 2.3 API 统一前缀

```text
/api/v1
```

## 2.4 API 成功返回格式

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

## 2.5 API 失败返回格式

```json
{
  "code": 40001,
  "message": "error message",
  "data": null
}
```

## 2.6 分页格式

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [],
    "total": 0,
    "page": 1,
    "page_size": 20
  }
}
```

## 2.7 Git 约束

```text
main
develop
feature/*
fix/*
```

所有开发必须：

```text
feature 分支
    ↓
Pull Request
    ↓
Code Review
    ↓
develop
    ↓
测试
    ↓
main
```

禁止直接在 main 开发。

---

# 三、人员 A：前端负责人

人员 A 负责：

```text
1. 前端项目骨架
2. 登录与主框架
3. 用户 / 组织 / 权限页面
4. 项目 / 项目成员页面
5. 任务管理页面
6. 任务共享看板
7. 人力预约与预约确认页面
8. 任务执行填报页面
9. 项目过程管理报表
10. 达成率 / 达成质量
11. 操作日志页面
12. 前后端联调
```

---

# A-01 前端项目骨架

## 目标

建立完整、规范、可供多人持续开发的 Vue 3 前端工程。

## 提示词

```text
你现在是本项目的前端架构工程师。

我要开发一个“项目任务与人力协同管理系统 V1.0”。

请使用以下技术栈：

- Vue 3
- TypeScript
- Vite
- Vue Router
- Pinia
- Axios
- Element Plus
- Tailwind CSS
- Day.js

请帮我从零设计并生成前端项目骨架。

系统主要模块包括：

1. 登录
2. Dashboard
3. 用户管理
4. 组织管理
5. 角色权限
6. 项目管理
7. 项目成员管理
8. 任务管理
9. 任务共享看板
10. 人力预约
11. 任务执行
12. 项目过程管理报表
13. 达成率 / 达成质量
14. 操作日志

要求：

1. 使用 Vue 3 Composition API。
2. 全部使用 TypeScript。
3. 目录结构必须适合多人协作。
4. API 调用统一放到 src/api。
5. 页面统一放到 src/views。
6. 公共组件放到 src/components。
7. Pinia Store 放到 src/stores。
8. TypeScript 类型放到 src/types。
9. 所有后端 API 前缀为 /api/v1。
10. 使用 Axios 封装统一请求。
11. 支持 JWT Token 自动携带。
12. 401 时跳转登录。
13. 页面设计风格要求：
    - 极简
    - 专业
    - 低饱和度
    - Apple 风格
    - 适合企业内部管理系统
14. 请不要一次写复杂业务页面，先把项目基础骨架搭好。
15. 不要引入 Vue 3、Vite、Element Plus、Pinia、Axios、Day.js、Tailwind CSS 之外的不必要大型依赖。

请输出：

第一部分：完整目录树。
第二部分：package.json。
第三部分：vite.config.ts。
第四部分：main.ts。
第五部分：App.vue。
第六部分：router/index.ts。
第七部分：stores/user.ts 基础结构。
第八部分：api/request.ts Axios 封装。
第九部分：layouts/MainLayout.vue。
第十部分：README.md 的启动说明。

每个文件都要给出完整代码，不要只给伪代码。
```

## 验收标准

- `npm install` 可安装。
- `npm run dev` 可启动。
- `/login` 可访问。
- 主布局可正常展示。
- Axios 可统一访问 `/api/v1`。
- 目录清晰，后续页面可以直接添加。

---

# A-02 登录页与主框架

## 提示词

```text
请基于现有 Vue 3 + TypeScript + Vite 项目，开发“项目任务与人力协同管理系统”的登录与主框架。

后端接口：

POST /api/v1/auth/login

请求：

{
  "username": "admin",
  "password": "123456"
}

返回：

{
  "code": 0,
  "message": "success",
  "data": {
    "access_token": "xxx",
    "token_type": "bearer",
    "user": {
      "id": 1,
      "name": "管理员",
      "role": "super_admin"
    }
  }
}

另外：

GET /api/v1/auth/me

用于获取当前登录用户。

请完成：

1. Login.vue
2. MainLayout.vue
3. 左侧 Sidebar
4. 顶部 Header
5. Breadcrumb
6. Router Guard
7. Pinia 用户状态
8. Token 存储
9. Axios 自动携带 Token
10. 401 自动退出并跳转登录

左侧菜单暂时包含：

- 首页
- 用户管理
- 组织管理
- 角色权限
- 项目管理
- 任务管理
- 任务共享看板
- 任务执行
- 项目过程报表
- 操作日志

UI 要求：

- Apple 风格
- 极简
- 轻阴影
- 大量留白
- 圆角
- 低饱和蓝灰色
- 不使用复杂渐变
- 不堆砌图标

请按以下顺序输出：

1. 需要新增/修改的文件列表
2. 每个文件完整代码
3. 路由结构
4. 登录流程说明
5. 运行测试步骤

不要修改未涉及模块。
```

---

# A-03 用户、组织、权限前端

## 提示词

```text
请开发“项目任务与人力协同管理系统 V1.0”的基础管理前端页面。

技术栈：

Vue 3 + TypeScript + Element Plus + Axios。

需要开发：

一、用户管理
字段：
- id
- username
- name
- email
- department
- role
- status
- created_at

功能：
- 用户列表
- 搜索
- 分页
- 新增用户
- 编辑用户
- 启用/禁用

接口：
GET /api/v1/users
POST /api/v1/users
GET /api/v1/users/{id}
PUT /api/v1/users/{id}

二、组织管理
展示：
- 部门
- L1
- L2
- L3
- L4
- 主管关系

要求使用树形结构显示组织层级。

三、角色权限
角色：
- 超级管理员
- 部门主管
- 职能主管
- 项目经理
- 项目成员

权限页面至少展示：
- 查看共享看板
- 编辑共享看板
- 查看任务执行报表
- 编辑任务执行
- 查看项目过程报表

要求：

1. 将 API 分为：
   - api/user.ts
   - api/organization.ts
   - api/role.ts
2. 页面分别放在：
   - views/user
   - views/organization
   - views/role
3. TypeScript 类型单独定义。
4. 表格支持 loading。
5. API 出错有统一提示。
6. 新增/编辑使用 Dialog。
7. 不要把全部逻辑写在一个 Vue 文件。
8. 页面风格与现有主框架一致。
9. 所有代码均给出完整版本。

最后给出：
- 文件结构
- 页面代码
- API 文件
- 类型文件
- 路由配置
- 测试步骤
```

---

# A-04 项目与项目成员前端

## 提示词

```text
请开发“项目任务与人力协同管理系统 V1.0”的项目管理前端。

项目字段：

- id
- code
- name
- project_type
- manager_id
- manager_name
- department_id
- status
- planned_start
- planned_end
- actual_start
- actual_end
- priority
- description

项目状态：

- Draft
- Planned
- Running
- Suspended
- Completed
- Cancelled

需要页面：

1. 项目列表
2. 新建项目
3. 编辑项目
4. 项目详情
5. 项目成员管理

接口：

GET /api/v1/projects
POST /api/v1/projects
GET /api/v1/projects/{id}
PUT /api/v1/projects/{id}

项目成员：

POST /api/v1/projects/{id}/members
DELETE /api/v1/projects/{id}/members/{user_id}

项目详情页需要分 Tabs：

- 基本信息
- 项目成员
- 项目任务
- 项目排期

要求：

1. Vue 3 + TypeScript。
2. 使用 Element Plus。
3. 日期使用 Day.js。
4. 表单必须进行必填验证。
5. 项目成员选择使用用户下拉搜索。
6. 状态使用 Tag 展示。
7. 不要在页面内直接拼 URL，API 统一封装。
8. 页面布局简洁。
9. 请输出完整代码和新增文件路径。
```

---

# A-05 任务管理前端

## 提示词

```text
请开发“项目任务与人力协同管理系统 V1.0”的任务管理页面。

任务结构支持：

项目
  ↓
一级任务
  ↓
二级任务

任务字段：

- id
- project_id
- parent_id
- name
- task_type
- owner_id
- owner_name
- planned_start
- planned_end
- estimated_hours
- status
- priority
- description

任务类型：

- Project
- Routine
- Training
- Leave
- Other

任务状态：

- 未开始
- 待确认
- 已确认
- 进行中
- 已完成
- 已延期
- 已取消

接口：

GET /api/v1/tasks
POST /api/v1/tasks
GET /api/v1/tasks/{id}
PUT /api/v1/tasks/{id}

页面需要：

1. 项目筛选。
2. 负责人筛选。
3. 状态筛选。
4. 任务列表。
5. 一级 / 二级层级展示。
6. 新增任务。
7. 编辑任务。
8. 查看详情。
9. 设置负责人。
10. 设置计划时间。
11. 设置预计工时。

要求：

- Vue 3 + TypeScript
- API 独立为 api/task.ts
- 类型独立为 types/task.ts
- 支持树形表格或层级缩进
- 不做复杂甘特图
- 时间字段统一格式
- 提供完整代码
```

---

# A-06 任务共享看板前端

## 提示词

```text
这是系统 V1.0 最核心的前端页面。

请设计并实现“任务共享看板”。

业务形式：

纵轴 = 人员
横轴 = 日期 + 小时

系统需要以“人员 × 时间”的形式展示每个人的任务和人力排期。

排期数据示例：

{
  "id": 1,
  "user_id": 1001,
  "user_name": "张三",
  "project_id": 2001,
  "project_name": "项目A",
  "task_id": 3001,
  "task_name": "任务1",
  "task_type": "Project",
  "start_time": "2026-10-01 09:00:00",
  "end_time": "2026-10-01 12:00:00",
  "status": "confirmed"
}

接口：

GET /api/v1/schedules

支持查询参数：

- start_date
- end_date
- project_id
- user_id
- department_id

V1.0 要求：

1. 按天/周展示。
2. 最小时间粒度 1 小时。
3. 纵轴显示人员。
4. 横轴显示日期和小时。
5. 根据 start_time 和 end_time 绘制排期块。
6. Project、Routine、Training、Leave 要能够区分。
7. 点击排期块显示详情。
8. 点击空白时间段可以打开“创建预约”弹窗。
9. 支持项目筛选。
10. 支持人员筛选。
11. 支持日期筛选。
12. 冲突记录使用醒目标识。
13. 不要在 V1.0 做复杂拖拽。
14. 不要引入大型甘特图库，优先使用 Vue + CSS Grid 自己实现。
15. 要考虑横向滚动。
16. 要考虑人员较多时的纵向滚动。
17. 人员名称列保持固定。
18. 时间表头保持固定。

请重点保证：
- 结构清晰
- 性能可接受
- 后续可扩展拖拽
- 每个文件职责单一

请输出：
1. 页面设计思路
2. 数据结构
3. 组件拆分
4. 完整代码
5. CSS Grid 计算方式
6. 排期块定位逻辑
7. API 对接
8. 测试数据
9. 边界情况
```

---

# A-07 人力预约与确认前端

## 提示词

```text
请开发“人力预约”前端功能。

预约字段：

- project_id
- task_id
- user_id
- start_time
- end_time
- planned_hours
- status
- remark

状态：

Draft
Pending
Confirmed
Rejected
Changed
Completed

接口：

POST /api/v1/schedules
PUT /api/v1/schedules/{id}
POST /api/v1/schedules/{id}/confirm
POST /api/v1/schedules/{id}/reject

需要功能：

管理人员：
1. 新建预约。
2. 修改预约。
3. 查看预约详情。
4. 提交预约。

项目成员：
1. 查看待确认预约。
2. 同意。
3. 拒绝。
4. 填写拒绝原因。

如果后端返回：

HTTP 409

{
  "code": 40901,
  "message": "schedule conflict",
  "data": {
    "conflicts": [...]
  }
}

前端必须展示：
- 冲突人员
- 冲突项目
- 冲突任务
- 冲突时间段

要求：
- Vue 3 + TypeScript
- Dialog 表单
- 时间合法性校验
- end_time 必须晚于 start_time
- 项目/任务/人员级联选择
- 错误提示清晰
- 给出完整代码
```

---

# A-08 任务执行填报前端

## 提示词

```text
请开发“我的任务”和“任务执行填报”页面。

执行记录字段：

- task_id
- user_id
- actual_start
- actual_end
- actual_hours
- status
- description
- exception_reason

接口：

GET /api/v1/executions
POST /api/v1/executions
PUT /api/v1/executions/{id}

需要页面：

1. 我的任务列表。
2. 当前进行中任务。
3. 待填写实际工时任务。
4. 任务执行详情。
5. 实际开始时间。
6. 实际结束时间。
7. 实际工时。
8. 执行说明。
9. 异常原因。

要求：

- 表单进行时间校验。
- actual_end 不能早于 actual_start。
- actual_hours 必须 >= 0。
- 可以从 planned_start / planned_end 显示计划对比。
- 页面简洁。
- API、types、view 分层。
- 给出完整代码。
```

---

# A-09 项目过程报表与评价前端

## 提示词

```text
请开发“项目过程管理报表”。

后端接口：

GET /api/v1/reports/process

返回字段：

- project_id
- project_name
- level1_task
- level2_task
- owner_name
- planned_start
- planned_end
- actual_start
- actual_end
- estimated_hours
- actual_hours
- achievement_rate
- achievement_quality

页面要求：

1. 项目筛选。
2. 人员筛选。
3. 日期筛选。
4. 表格展示。
5. 计划与实际时间对比。
6. 预计工时与实际工时对比。
7. 达成率显示。
8. 达成质量显示。
9. 项目经理可以编辑达成率。
10. 项目经理可以编辑达成质量。

V1.0 不做复杂图表。

要求：
- Vue 3 + TypeScript
- Element Plus Table
- 固定重要列
- 宽表横向滚动
- 百分比格式统一
- 日期格式统一
- 代码完整
```

---

# A-10 操作日志前端

## 提示词

```text
请开发系统的“操作日志”页面。

日志字段：

- id
- operator_name
- module
- action
- object_type
- object_id
- before_data
- after_data
- reason
- created_at

接口：

GET /api/v1/operation-logs

页面要求：

1. 按操作人筛选。
2. 按模块筛选。
3. 按时间筛选。
4. 分页。
5. 点击查看修改前和修改后。
6. JSON 数据使用格式化方式展示。
7. 共享看板修改记录必须能够清晰查看。

要求：
- 不允许普通成员随意查看全部日志。
- 前端根据权限控制菜单和按钮。
- 提供完整实现代码。
```

---

# 四、人员 B：基础后端 / 架构 / 数据库负责人

B 的任务：

```text
1. FastAPI 项目骨架
2. MySQL + SQLAlchemy + Alembic
3. 登录认证
4. 用户管理
5. 组织管理
6. RBAC
7. 项目管理
8. 项目成员管理
9. 操作日志基础能力
10. 统一异常与响应
11. 部署基础配置
```

---

# B-01 FastAPI 后端项目骨架

## 提示词

```text
你现在是本项目的后端架构负责人。

我要开发“项目任务与人力协同管理系统 V1.0”。

技术栈：

Python 3.11+
FastAPI
Pydantic
SQLAlchemy
Alembic
MySQL 8
JWT
Pytest

请设计一套适合 3 人协作开发的后端工程骨架。

要求目录：

backend/
├── app/
│   ├── main.py
│   ├── api/
│   │   └── v1/
│   ├── models/
│   ├── schemas/
│   ├── services/
│   ├── repositories/
│   ├── core/
│   └── utils/
├── alembic/
├── tests/
├── requirements.txt
└── README.md

要求：

1. API 前缀 /api/v1。
2. 支持 CORS。
3. 统一异常处理。
4. 统一返回结构。
5. 配置通过环境变量读取。
6. MySQL 连接配置独立。
7. 支持 Swagger。
8. 提供 /health 健康检查。
9. 不要把业务代码写在 main.py。
10. router/service/repository/model/schema 分层。
11. requirements.txt 明确版本范围。
12. 提供启动命令。

请输出：
- 完整目录
- 所有基础文件完整代码
- requirements.txt
- .env.example
- README
```

---

# B-02 数据库设计与 Alembic

## 提示词

```text
请为“项目任务与人力协同管理系统 V1.0”设计 MySQL 数据库。

一期核心实体：

1. users
2. departments
3. organizations
4. roles
5. permissions
6. user_roles
7. role_permissions
8. projects
9. project_members
10. tasks
11. schedule_bookings
12. execution_records
13. task_evaluations
14. risk_records
15. operation_logs

请完成：

第一部分：
画出文字版 ER 关系。

第二部分：
为每张表设计：
- 字段
- MySQL 类型
- 主键
- 外键
- 非空
- 默认值
- 唯一约束
- 索引

第三部分：
说明为什么这样设计。

第四部分：
生成 SQLAlchemy 2.x Model。

第五部分：
初始化 Alembic。

第六部分：
生成第一版 migration。

特别要求：

1. 时间字段统一使用 datetime。
2. 所有主表包含 created_at / updated_at。
3. 用户名唯一。
4. 项目 code 唯一。
5. schedule_bookings 必须为冲突查询设计索引。
6. 任务支持 parent_id。
7. 不使用数据库 ENUM，优先字符串 + 应用层校验，便于后期扩展。
8. 删除策略需要说明。
9. 不做过度复杂设计。
10. 所有代码完整。
```

---

# B-03 登录认证与 JWT

## 提示词

```text
请为 FastAPI 项目实现登录认证。

接口：

POST /api/v1/auth/login

GET /api/v1/auth/me

需求：

1. 用户名 + 密码登录。
2. 密码禁止明文存储。
3. 使用安全哈希。
4. 登录成功返回 JWT access_token。
5. /auth/me 返回当前用户。
6. 禁用用户禁止登录。
7. 提供 get_current_user 依赖。
8. 需要保护的 API 通过 Depends 鉴权。
9. Token 过期返回 401。
10. 统一错误结构。

请生成：

- schemas/auth.py
- services/auth_service.py
- core/security.py
- api/v1/auth.py
- 所需依赖
- 测试代码

请不要把密码放进日志。
```

---

# B-04 用户与组织后端

## 提示词

```text
请实现 V1.0 用户与组织管理后端。

用户接口：

GET /api/v1/users
POST /api/v1/users
GET /api/v1/users/{id}
PUT /api/v1/users/{id}

用户字段：

- username
- name
- email
- department_id
- organization_id
- supervisor_id
- status

组织需要支持：

- Department
- L1
- L2
- L3
- L4
- Parent / Child

要求：

1. 支持分页。
2. 支持 name / username 搜索。
3. 支持 department 筛选。
4. username 唯一。
5. supervisor_id 必须引用有效用户。
6. 组织使用树结构。
7. service 和 repository 分层。
8. Pydantic 分 Create / Update / Response。
9. 提供完整 CRUD。
10. 写 Pytest 基础测试。
```

---

# B-05 RBAC 权限系统

## 提示词

```text
请实现本系统 V1.0 的 RBAC 权限模型。

角色：

- super_admin
- department_manager
- functional_manager
- project_manager
- project_member

权限至少包含：

- user:view
- user:edit
- organization:view
- organization:edit
- project:view
- project:edit
- schedule:view
- schedule:edit
- execution:view
- execution:edit
- process_report:view
- evaluation:edit
- operation_log:view

数据库：

roles
permissions
user_roles
role_permissions

请实现：

1. 创建默认角色。
2. 创建默认权限。
3. 用户分配角色。
4. 角色分配权限。
5. FastAPI 权限依赖：
   require_permission("project:edit")
6. super_admin 自动拥有全部权限。
7. 当前用户权限查询。
8. 权限不足返回 403。
9. API 与 Service 分层。
10. 初始化脚本。

请给出完整代码，并说明如何在其他接口中使用。
```

---

# B-06 项目管理后端

## 提示词

```text
请实现“项目管理”后端。

项目字段：

- code
- name
- project_type
- manager_id
- department_id
- status
- planned_start
- planned_end
- actual_start
- actual_end
- priority
- description

状态：

Draft
Planned
Running
Suspended
Completed
Cancelled

接口：

GET /api/v1/projects
POST /api/v1/projects
GET /api/v1/projects/{id}
PUT /api/v1/projects/{id}

要求：

1. code 唯一。
2. manager_id 必须是有效用户。
3. planned_end >= planned_start。
4. 支持状态筛选。
5. 支持项目名称搜索。
6. 支持项目经理筛选。
7. 支持分页。
8. 项目经理只能编辑自己有权限的项目。
9. 部门主管可查看部门项目。
10. super_admin 可查看全部项目。
11. 按项目权限进行数据过滤。
12. 给出完整代码和测试。
```

---

# B-07 项目成员后端

## 提示词

```text
请实现项目成员管理。

表：

project_members

字段：

- id
- project_id
- user_id
- project_role
- allocation_percent
- joined_at
- left_at

接口：

GET /api/v1/projects/{id}/members
POST /api/v1/projects/{id}/members
DELETE /api/v1/projects/{id}/members/{user_id}

要求：

1. 同一用户不能重复加入同一项目。
2. allocation_percent 范围 0~100。
3. 项目不存在返回 404。
4. 用户不存在返回 404。
5. 项目经理可以管理自己的项目成员。
6. 成员删除建议逻辑删除或保留历史关系。
7. 项目成员信息需要可被 C 的任务和排期模块查询。
8. 请输出 Model / Schema / Repository / Service / Router / Test。
```

---

# B-08 统一异常与响应结构

## 提示词

```text
请为整个 FastAPI 项目建立统一响应和异常体系。

成功格式：

{
  "code": 0,
  "message": "success",
  "data": {}
}

常见错误：

400 参数错误
401 未登录
403 无权限
404 数据不存在
409 业务冲突
422 参数校验错误
500 系统错误

请实现：

1. 通用 ResponseModel。
2. 分页 Response。
3. BusinessException。
4. 全局异常 handler。
5. Pydantic 校验异常转换。
6. HTTPException 转换。
7. 数据库异常保护。
8. 日志记录。
9. 不向前端暴露数据库堆栈。
10. 给出项目中如何调用的示例。
```

---

# B-09 操作日志基础能力

## 提示词

```text
请为系统实现统一操作日志。

operation_logs 字段：

- id
- operator_id
- module
- action
- object_type
- object_id
- before_data
- after_data
- reason
- ip_address
- created_at

需要记录：

- 创建项目
- 修改项目
- 新增项目成员
- 删除项目成员
- 创建任务
- 修改任务
- 创建预约
- 修改预约
- 确认预约
- 拒绝预约
- 填写执行记录
- 修改达成率
- 修改达成质量

要求：

1. 提供 log_operation() 通用函数或 Service。
2. before_data / after_data 使用 JSON。
3. 日志写入失败不能导致主业务整体失败，但必须记录系统错误。
4. GET /api/v1/operation-logs 支持分页。
5. 支持 operator/module/date 筛选。
6. operation_log:view 权限才能查看。
7. C 的业务模块可以直接复用。
8. 给出完整代码。
```

---

# B-10 后端部署配置

## 提示词

```text
请为 FastAPI + MySQL + Vue 前后端分离项目编写 V1.0 部署方案。

服务器：

Linux

架构：

Browser
  ↓
Nginx
  ├── Vue dist
  └── /api → FastAPI
                ↓
              MySQL

请生成：

1. backend Dockerfile
2. frontend Dockerfile（如果采用 Docker）
3. docker-compose.yml
4. nginx.conf
5. .env.example
6. FastAPI 启动配置
7. 数据库 migration 启动步骤
8. 日志目录建议
9. 静态文件路径
10. 部署命令
11. 回滚方法
12. 健康检查方法

如果不使用 Docker，也给出 Linux 原生部署方式。

不要使用 Kubernetes。
```

---

# 五、人员 C：业务后端负责人

C 负责：

```text
1. 任务管理
2. 人力排期
3. 人力预约
4. 冲突检测
5. 预约确认
6. 执行记录
7. 工时
8. 项目过程报表
9. 达成率 / 达成质量
10. V1.0 基础风险判断
```

---

# C-01 任务管理后端

## 提示词

```text
请实现 V1.0 任务管理后端。

任务支持：

项目
  ↓
一级任务
  ↓
二级任务

字段：

- id
- project_id
- parent_id
- name
- task_type
- owner_id
- planned_start
- planned_end
- estimated_hours
- status
- priority
- description
- created_at
- updated_at

task_type：

Project
Routine
Training
Leave
Other

status：

not_started
pending
confirmed
running
completed
delayed
cancelled

接口：

GET /api/v1/tasks
POST /api/v1/tasks
GET /api/v1/tasks/{id}
PUT /api/v1/tasks/{id}

要求：

1. project_id 必须存在。
2. owner_id 必须是有效用户。
3. parent_id 必须属于同一项目。
4. 禁止形成循环父子关系。
5. planned_end >= planned_start。
6. estimated_hours >= 0。
7. 支持 project_id/owner_id/status 查询。
8. 支持分页。
9. Service / Repository 分层。
10. 接入 B 提供的权限与操作日志。
11. 给出完整测试。
```

---

# C-02 人力排期模型与 CRUD

## 提示词

```text
请实现 schedule_bookings 人力排期模块。

字段：

- id
- user_id
- project_id
- task_id
- start_time
- end_time
- planned_hours
- status
- remark
- created_by
- created_at
- updated_at

状态：

draft
pending
confirmed
rejected
changed
running
completed
cancelled

接口：

GET /api/v1/schedules
POST /api/v1/schedules
GET /api/v1/schedules/{id}
PUT /api/v1/schedules/{id}
DELETE /api/v1/schedules/{id}

查询支持：

- start_date
- end_date
- user_id
- project_id
- task_id
- department_id
- status

要求：

1. end_time > start_time。
2. planned_hours 可由时间差自动计算。
3. user 必须存在。
4. project 必须存在。
5. task 必须属于 project。
6. 只有 draft / rejected 状态允许自由编辑。
7. confirmed 修改后状态变 changed，并要求重新确认。
8. 删除只允许 draft 或 cancelled。
9. 接入操作日志。
10. 完整代码与测试。
```

---

# C-03 人力冲突检测

## 提示词

```text
请为 schedule_bookings 实现核心的“人员时间冲突检测”。

业务规则：

同一用户的两个有效排期，如果时间存在交集，则视为冲突。

冲突公式：

new_start < existing_end
AND
new_end > existing_start

需要参与检测的状态：

pending
confirmed
changed
running

不参与检测：

rejected
cancelled
completed

例如：

已有：
09:00 - 12:00

新增：
10:00 - 11:00

=> 冲突

已有：
09:00 - 12:00

新增：
12:00 - 14:00

=> 不冲突

要求：

1. 在创建预约前检查。
2. 修改预约前检查。
3. 修改时排除当前记录本身。
4. 一次返回全部冲突记录。
5. 返回 HTTP 409。
6. 返回格式：

{
  "code": 40901,
  "message": "schedule conflict",
  "data": {
    "conflicts": [
      {
        "schedule_id": 1,
        "project_id": 10,
        "project_name": "项目A",
        "task_id": 20,
        "task_name": "任务1",
        "start_time": "...",
        "end_time": "..."
      }
    ]
  }
}

7. SQL 查询必须利用索引。
8. 给出 Repository 查询方法。
9. 给出 Service。
10. 给出 Router 集成方式。
11. 编写至少 8 个 Pytest 用例，覆盖：
    - 完全包含
    - 左侧相交
    - 右侧相交
    - 完全覆盖
    - 边界相接
    - 不相交
    - 修改自身
    - cancelled 不冲突
```

---

# C-04 预约确认与拒绝

## 提示词

```text
请实现“人力预约确认”业务。

接口：

POST /api/v1/schedules/{id}/confirm

POST /api/v1/schedules/{id}/reject

确认规则：

pending → confirmed
changed → confirmed

拒绝规则：

pending → rejected
changed → rejected

请求：

{
  "reason": "..."
}

要求：

1. 只有预约对象本人或具备指定权限的人可以确认/拒绝。
2. confirmed 之前再次执行冲突检测。
3. rejected 必须允许填写 reason。
4. 不允许对 completed/cancelled 重复确认。
5. 状态转换必须集中在 Service，不要散落在 Router。
6. 所有状态变更写操作日志。
7. 提供状态机说明。
8. 给出完整代码与测试。
```

---

# C-05 任务执行记录

## 提示词

```text
请实现 execution_records 任务执行模块。

字段：

- id
- task_id
- user_id
- actual_start
- actual_end
- actual_hours
- status
- description
- exception_reason
- created_at
- updated_at

接口：

GET /api/v1/executions
POST /api/v1/executions
GET /api/v1/executions/{id}
PUT /api/v1/executions/{id}

要求：

1. task 必须存在。
2. user 必须存在。
3. 用户只能为自己填执行记录，管理人员除外。
4. actual_end >= actual_start。
5. actual_hours >= 0。
6. actual_hours 可以根据时间自动计算，也允许人工调整。
7. 支持 task_id/user_id/project_id/date 查询。
8. 支持“我的执行记录”。
9. 更新任务完成情况时不能直接破坏任务主数据。
10. 接入操作日志。
11. 给出完整测试。
```

---

# C-06 项目过程管理报表接口

## 提示词

```text
请实现项目过程管理报表接口：

GET /api/v1/reports/process

报表需要整合：

projects
tasks
users
schedule_bookings
execution_records
task_evaluations

返回字段：

- project_id
- project_name
- level1_task
- level2_task
- owner_id
- owner_name
- planned_start
- planned_end
- actual_start
- actual_end
- estimated_hours
- actual_hours
- achievement_rate
- achievement_quality

查询支持：

- project_id
- owner_id
- start_date
- end_date

要求：

1. 报表查询不要在 Router 拼接。
2. Repository 专门负责聚合查询。
3. Service 负责字段转换和业务计算。
4. 没有 execution record 时 actual 字段允许为空。
5. 一级任务 / 二级任务正确显示。
6. 支持分页。
7. 注意避免 N+1 查询。
8. 给出 SQLAlchemy 查询方案。
9. 给出完整代码。
10. 给出测试数据和返回示例。
```

---

# C-07 达成率与达成质量

## 提示词

```text
请实现 task_evaluations 模块。

V1.0 采用“人工评价为主”。

字段：

- id
- task_id
- evaluator_id
- achievement_rate
- achievement_quality
- comment
- evaluated_at

achievement_rate：

0 ~ 100

achievement_quality：

0 ~ 100

接口：

GET /api/v1/tasks/{id}/evaluation
PUT /api/v1/tasks/{id}/evaluation

要求：

1. 只有项目经理、部门主管或具备 evaluation:edit 权限的人可以修改。
2. achievement_rate 范围 0~100。
3. achievement_quality 范围 0~100。
4. 同一任务只保留当前有效评价，也可以通过操作日志追溯历史。
5. 修改前后写操作日志。
6. 报表接口自动关联评价。
7. 给出完整代码与测试。
```

---

# C-08 V1.0 延期判断

## 提示词

```text
请为 V1.0 实现基础任务延期判断。

规则：

当前时间 > planned_end
AND
task.status != completed
AND
task.status != cancelled

则任务视为 delayed。

要求：

1. 不需要 Celery。
2. 不需要后台定时任务。
3. V1.0 可以在查询任务/报表时动态计算 delayed。
4. 不要频繁写数据库。
5. 给出：
   - Python 判断函数
   - Service 集成方式
   - API 返回方式
   - 测试用例
6. 说明什么时候需要在二期升级成 risk_records + 定时检测。
```

---

# C-09 V1.0 人员过载计算

## 提示词

```text
请为 V1.0 实现简单人员负载计算。

规则：

以每天标准可用工时 8 小时为例。

统计 confirmed/running 的 schedule_bookings。

每日：

planned_hours 总和 / 8小时 × 100%

返回：

- user_id
- user_name
- date
- planned_hours
- available_hours
- load_rate
- overloaded

判断：

load_rate > 100%
=> overloaded = true

接口：

GET /api/v1/reports/workload

查询：

- start_date
- end_date
- department_id
- user_id

V1.0 不做复杂预测。

要求：

1. SQL 聚合优先。
2. 避免逐人循环大量查询。
3. 说明时区处理。
4. 给出 Repository / Service / Router。
5. 给出测试。
```

---

# 六、三个人共同任务提示词

以下任务不是某一个人独立完成，需要三个人共同执行。

---

# TEAM-01 需求冻结提示词

```text
你现在是本项目的产品经理 + 技术架构师。

请基于“项目任务与人力协同管理系统 V1.0”帮助我们做一次需求冻结。

V1.0 功能：

1. 登录
2. 用户
3. 组织
4. RBAC
5. 项目
6. 项目成员
7. 任务
8. 共享看板
9. 人力预约
10. 预约确认
11. 人力冲突检测
12. 任务执行
13. 项目过程报表
14. 达成率
15. 达成质量
16. 操作日志

请输出：

1. 功能清单
2. 每个功能必须实现的最小能力
3. 明确“不在 V1.0 做”的内容
4. 业务名词定义
5. 状态枚举
6. 关键业务规则
7. 功能之间依赖关系
8. V1.0 验收标准

目标：
避免开发过程中不断增加功能。
```

---

# TEAM-02 API 评审提示词

```text
我们是三人开发团队：

A 前端
B 基础后端
C 业务后端

请对 V1.0 API 进行接口评审。

已有主要接口：

POST /api/v1/auth/login
GET /api/v1/auth/me

GET/POST/PUT /api/v1/users

GET/POST/PUT /api/v1/projects

GET/POST/PUT /api/v1/tasks

GET/POST/PUT /api/v1/schedules

POST /api/v1/schedules/{id}/confirm
POST /api/v1/schedules/{id}/reject

GET/POST/PUT /api/v1/executions

GET /api/v1/reports/process
GET /api/v1/reports/workload

GET/PUT /api/v1/tasks/{id}/evaluation

GET /api/v1/operation-logs

请检查：

1. RESTful 是否合理。
2. 命名是否一致。
3. 请求字段是否一致。
4. 返回格式是否一致。
5. 状态码是否合理。
6. 是否存在前后端难以联调的问题。
7. 是否缺接口。
8. 是否有重复接口。
9. 给出最终 API 清单。
10. 给出每个 API 的 request/response 示例。

只围绕 V1.0，不扩展二期功能。
```

---

# TEAM-03 数据库评审提示词

```text
请作为数据库架构师，对以下 V1.0 表设计做评审：

users
departments
organizations
roles
permissions
user_roles
role_permissions
projects
project_members
tasks
schedule_bookings
execution_records
task_evaluations
risk_records
operation_logs

请重点检查：

1. 主键。
2. 外键。
3. 唯一约束。
4. 索引。
5. 时间字段。
6. 状态字段。
7. 任务父子关系。
8. 排期冲突查询性能。
9. 数据删除策略。
10. 是否存在重复数据。
11. 是否符合 1NF / 2NF / 3NF 的基本原则。
12. V1.0 是否存在过度设计。

最后给出：
- 必须修改
- 建议修改
- 可以保留
三类结论。
```

---

# TEAM-04 前后端联调提示词

```text
我们正在进行 Vue 3 + FastAPI 项目的前后端联调。

请根据我接下来提供的：

1. 前端请求代码
2. 后端 Router
3. Pydantic Schema
4. 实际接口返回
5. 浏览器 Network 错误

帮我严格从以下顺序排查：

第一步：URL
第二步：HTTP Method
第三步：请求 Header
第四步：JWT
第五步：Content-Type
第六步：请求参数
第七步：Pydantic Schema
第八步：后端业务逻辑
第九步：数据库
第十步：响应结构
第十一步：前端 TypeScript 类型

要求：

- 不要一开始就重构代码。
- 优先定位真正故障点。
- 每一步告诉我怎么验证。
- 给出最小修改方案。
```

---

# TEAM-05 V1.0 测试提示词

```text
请为“项目任务与人力协同管理系统 V1.0”生成完整测试清单。

模块：

1. 登录
2. 用户
3. 组织
4. 权限
5. 项目
6. 项目成员
7. 任务
8. 共享看板
9. 人力预约
10. 冲突检测
11. 预约确认
12. 任务执行
13. 项目过程报表
14. 达成率
15. 达成质量
16. 操作日志

测试分类：

- 正常流程
- 边界条件
- 权限
- 参数校验
- 时间冲突
- 数据一致性
- 并发基础场景
- 页面显示
- API
- 数据库

请输出表格：

编号
模块
测试场景
前置条件
步骤
预期结果
优先级
负责人建议

重点把 schedule 时间冲突的测试写完整。
```

---

# 七、推荐实际执行顺序

不要同时把全部提示词扔给 AI。

建议按照下面顺序执行。

## 第 1 阶段：基础准备

```text
TEAM-01 需求冻结
        ↓
TEAM-03 数据库评审
        ↓
TEAM-02 API 评审
```

---

## 第 2 阶段：项目骨架

A：

```text
A-01
A-02
```

B：

```text
B-01
B-02
B-03
```

C：

```text
先参与数据库与 API 评审
```

---

## 第 3 阶段：基础业务

A：

```text
A-03
A-04
A-05
```

B：

```text
B-04
B-05
B-06
B-07
B-08
```

C：

```text
C-01
C-02
```

---

## 第 4 阶段：核心排期业务

A：

```text
A-06
A-07
```

C：

```text
C-03
C-04
```

这一步是整个 V1.0 的核心。

---

## 第 5 阶段：执行与报表

A：

```text
A-08
A-09
```

C：

```text
C-05
C-06
C-07
C-08
C-09
```

B：

```text
B-09
```

---

## 第 6 阶段：日志、联调、部署

A：

```text
A-10
```

B：

```text
B-10
```

三个人：

```text
TEAM-04
TEAM-05
```

---

# 八、推荐每个提示词附加的通用代码约束

为了防止 AI 每次生成不同风格代码，建议所有代码生成提示词末尾追加以下内容：

```text
【统一代码要求】

1. 不要生成伪代码。
2. 必须给出完整可运行代码。
3. 必须标明文件路径。
4. 不要随意修改已有目录结构。
5. 不要重新命名已经确定的字段。
6. 不要增加未要求的第三方依赖。
7. 不要修改已经确定的 API 路径。
8. 所有代码使用 UTF-8。
9. Python 使用类型注解。
10. TypeScript 禁止大量使用 any。
11. 后端 Router 不写复杂业务逻辑。
12. Service 负责业务。
13. Repository 负责数据库。
14. Pydantic Schema 与 SQLAlchemy Model 分离。
15. 前端 API 与页面分离。
16. 所有异常必须可读。
17. 关键业务必须写注释。
18. 给出启动或测试方法。
19. 如果依赖其他尚未实现模块，请明确列出依赖，不要自行假设。
20. 如果与既有代码存在冲突，优先保持既有架构，并说明修改点。
```

---

# 九、推荐 AI 代码生成工作方式

每完成一个模块，建议采用四步法。

## 第一步：让 AI 先设计

例如：

```text
先不要写代码。

请先分析任务共享看板应该拆成哪些 Vue 组件，
数据结构是什么，
API 需要什么字段，
状态如何管理。

等我确认后再写代码。
```

---

## 第二步：让 AI 写单一模块

不要：

```text
帮我把整个系统全部生成出来。
```

应该：

```text
现在只实现 schedule_bookings 后端，
其他模块都不要修改。
```

---

## 第三步：让 AI 自检

代码生成后继续：

```text
请检查你刚刚生成的代码：

1. 是否可以直接运行。
2. import 是否完整。
3. 路由是否正确。
4. Schema 是否与 Model 一致。
5. 是否有未定义变量。
6. 是否存在循环依赖。
7. 是否符合本项目统一返回格式。
8. 是否破坏已有接口。

只输出发现的问题和需要修改的代码。
```

---

## 第四步：再进行联调

```text
这是前端请求代码：

...

这是后端接口代码：

...

这是实际报错：

...

请只定位联调问题，不要重新设计整个系统。
```

---

# 十、V1.0 最终开发检查表

## A 前端

- [ ] 前端骨架
- [ ] 登录
- [ ] 主布局
- [ ] 用户
- [ ] 组织
- [ ] 权限
- [ ] 项目
- [ ] 项目成员
- [ ] 任务
- [ ] 共享看板
- [ ] 人力预约
- [ ] 预约确认
- [ ] 执行填报
- [ ] 过程报表
- [ ] 达成率 / 质量
- [ ] 操作日志
- [ ] 前端权限
- [ ] API 联调

## B 基础后端

- [ ] FastAPI 骨架
- [ ] MySQL
- [ ] SQLAlchemy
- [ ] Alembic
- [ ] Auth
- [ ] User
- [ ] Organization
- [ ] RBAC
- [ ] Project
- [ ] Project Member
- [ ] 统一响应
- [ ] 统一异常
- [ ] 操作日志
- [ ] 部署

## C 业务后端

- [ ] Task
- [ ] Schedule
- [ ] Schedule CRUD
- [ ] 冲突检测
- [ ] Confirm
- [ ] Reject
- [ ] Execution
- [ ] Process Report
- [ ] Evaluation
- [ ] Delay
- [ ] Workload

---

# 十一、最终原则

V1.0 的目标不是“做得最多”，而是：

> **让项目、任务、人员、排期、执行、评价形成完整闭环。**

三个人开发时最重要的六条原则：

```text
需求先冻结
数据库先确定
API先定义
模块独立开发
小步及时联调
所有变更可追溯
```

AI 的正确使用方式也不是一次性生成整个项目，而是：

```text
系统设计
   ↓
模块设计
   ↓
单模块代码
   ↓
自检
   ↓
联调
   ↓
测试
```

按照本文档中的提示词顺序执行，可以让 A、B、C 三个人在统一架构下并行完成 V1.0。
