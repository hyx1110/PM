# 更新记录

## 未发布

- 将任务计划起止和执行实际起止统一为按天保存，新增 `20260917_0011` 迁移；补齐项目、任务、执行及报表查询的开始/结束日期校验。
- 项目实际起止日期改由执行记录自动汇总，移除项目表单手工编辑；实际执行日期禁止填写未来日期，汇总任务禁止直接填报执行记录。
- 新增执行状态联动服务：执行记录创建、修改、删除后自动汇总任务、父任务和项目状态，多负责人任务只有全部负责人完成后才完成；“已延期”继续作为实时筛选状态。
- 修复“部分子任务已完成、其余未开始”时父任务和项目错误回退为未开始/已计划的问题，此类部分完成状态统一视为进行中。
- 明确两级任务边界：存在子任务的汇总任务不能直接预约或填报执行记录，已有有效预约/执行记录的任务不能再转换为汇总任务。
- 实际工时改为按所选工作日容量自动计算或校验，杜绝负数、零工时和超过工作日容量的填报。
- 项目、任务和执行常规列表分别收敛为本人管理项目、本人负责任务和本人执行记录；首页延期统计只覆盖本人任务及本人负责项目。
- 共享看板合并姓名/工号筛选，同项目统一配色、待确认预约统一灰色并加强跨日边界；项目经理点击本人空白时段可选择个人安排或预约本人项目工时。
- 共享看板补充按日程人员范围生成的项目筛选选项，修复指定单个人员后当前用户行仍被强制保留的问题。
- 项目/任务新增表单移除优先级；项目成员树简化组织标签并单独展示未分配组织人员；超级管理员禁止通过用户、角色或导入功能新增或移除，只能初始化配置。
- 部门负责人选择器只展示本部门且具有 L3 系统角色的有效用户，未分配组织的初始项目成员直接列在部门节点下。
- 组织树直接展示组织成员；右上角新增个人资料和密码修改；移除通知偏好接口、界面及运行时逻辑，临期阈值和外部渠道改由环境配置统一控制。
- 项目评价改为本项目项目经理在项目和任务完成后一次性提交，提交后显示“已完成评价”且禁止重复评价。
- 评价创建增加并发锁；项目产生评价后锁定执行记录，避免回改执行数据导致任务/项目状态回退而评价仍残留。
- 修正默认角色授权：`evaluation:edit` 从 L3 调整到项目经理，避免项目经理满足业务条件后仍被权限层拒绝。
- 操作日志增加字段级中文变更摘要；应用、异常、Uvicorn 访问日志及 Celery 后台任务日志统一按北京时间写入 `logs/YYYY-MM-DD/app-HH.log` 小时文件。
- 完成全项目静态逻辑审查：阻止最后一个超级管理员被禁用或移除角色，禁止直属主管循环和停用组织关系继续分配，并在变更用户部门/角色/状态前校验仍在生效的管理职责与业务依赖。
- 修复空角色列表可绕过用户接口移除初始化超级管理员的问题；角色变更日志现在记录变更前后的实际角色集合。
- 补齐项目、任务和预约状态完整性：限制任务及预约必须落在上级计划范围内，禁止非法终态回退，结束项目/任务或移除成员前校验活动依赖，并由预约待办、共享看板与 Celery 自动推进已确认预约的进行中/已完成状态及过期待确认预约的自动取消。
- 收紧 Excel 用户导入权限，只有超级管理员可批量创建用户和分配系统角色；同时修复停用组织导入、重复上级任务名称、通知外部投递队列饥饿和更新接口显式空值导致数据库 500 等问题。
- 将 `20260916_0010` 改为可恢复迁移，MySQL 非事务 DDL 中断后可安全补建索引并幂等回填任务负责人。
- 修复共享看板、项目详情、任务、执行、过程报表和操作日志选项超过单页上限后静默漏数据的问题，相关下拉项和详情数据改为完整分页读取。
- 修复 `20260916_0009` 在旧版 MySQL 上执行 `DROP CHECK` 语法失败的问题；迁移现会检查表、字段、索引和约束是否存在，可从 MySQL 非事务 DDL 导致的半完成状态继续。
- 按最新产品范围重组界面：首页直接展示“我的任务”和 L3 追加工时审批；通知只保留右上角入口；隐藏工作日历、人员负载和风险页面；移除经营分析页面及 API。
- 项目列表改为仅显示本人作为项目经理的项目，编号保存时自动生成，并增加部门、组织、员工号、姓名筛选。
- 任务列表改为仅显示本人作为负责人的任务；新增 `task_assignees` 多负责人关系及 `20260916_0010` 迁移，新增任务移除优先级。
- 共享看板增加项目、姓名、工号、部门、组织联合筛选，非筛选人员整行移除；L4 日程范围增加第二级下属；按钮预约与点击人员空白格采用不同的人员选择交互。
- 收紧越权：项目、任务、执行记录按本人关系展示和维护；评价仅允许本项目项目经理在项目已结束且任务已完成后提交。
- 新增用户强制选择部门，初始密码和确认密码显示必填；用户列表增加组织筛选；登录页展示明确的认证失败原因。
- MySQL 会话时区、业务时间与应用日志统一为北京时间。
- 修复保留的负载与风险后台逻辑的数据范围：L3/超级管理员保持全局范围，L4 使用本人、直属及第二级下属范围，项目经理使用本人负责项目与项目成员范围，普通成员仅限本人；这些页面仍不在 UI 展示。
- 任务 Excel 导入支持以逗号填写多名负责人，逐人校验有效项目成员关系、写入负责人关联并发送通知，同时阻止非本项目经理通过导入绕过任务创建权限。
- 任务 Excel 模板移除无效的初始状态列；用户导入对必填部门编码返回明确校验原因。
- 简化用户数据库：`employee_no` 成为唯一登录账号，删除 `username`、手机号、人员档案、人事职级以及角色授权来源字段；保留姓名、邮箱、部门、组织、直属主管和五类系统角色，并新增 `20260916_0009` 迁移。
- 优化共享日程预约状态配色：待确认/变更待确认统一灰色，已确认/进行中/已完成按项目稳定配色；拒绝、撤回、取消和遗留草稿只保留在历史记录中，不再显示在日、周、月日程表。
- 修复创建执行记录时 `actual_hours` 同时来自 Schema 展开参数和显式参数，导致 `ExecutionRecord()` 抛出重复关键字参数并返回 500 的问题；未传工时时仍由后端按实际起止时间计算，显式传入时保留传入值。
- 创建项目时强制指定至少一名初始成员，并将项目经理自动写入项目成员表且禁止移除；项目 Excel 模板同步增加成员员工号列。
- 收紧共享看板与预约权限：项目经理仅查看自己项目组成员并仅预约本人负责项目的有效成员；L3 可查看所有人员，L4 查看本人、直属下属及第二级下属；预约操作最终统一限定为项目经理对本人已审批项目的有效成员发起。
- 调整个人时间入口：所有角色点击自己的空白日程格都进入本人安排，新增独立的 `out_of_office` 外出状态，并可设置、撤回培训、会议、休假、外出、出差等安排；生效安排继续阻止任何重叠预约。
- 新增 `20260915_0008_project_manager_membership.py`，为历史项目补齐项目经理固定成员关系。
- 修复 Python 3.11 导入个人时间仓储时，类方法 `list` 遮蔽内置类型并导致 `TypeError: 'function' object is not subscriptable` 的启动错误。
- 历史 `0006/0007` 迁移曾试验人员档案、岗位、人事职级和角色来源；最终由 `0009` 删除，不再作为当前系统模型或 Excel 字段。
- 用户与项目/任务 Excel 模板统一改用员工号关联；项目和任务导入分别支持多名项目成员与多名任务负责人。
- 新增个人时间安排：所有用户均可设置并撤回自己的培训、会议、休假、外出、出差或其他占用；生效中的个人安排会显示在日/周/月共享看板，并在单条、批量、拖动及复制周预约时统一阻止时间重叠。
- 修复 GAP-002：共享看板人员姓名新增“查看全部安排”入口，点击后可分页查看该人员跨日期、跨项目的全部可见预约，并可继续打开单条预约详情。
- 优化人力预约表颜色：法定节假日使用独立红色系，普通周末及工作日上班前、午休、下班后使用灰色禁用背景，正常工作时间保持原样；调休工作日按工作日显示。
- 修复通知中心将单条或全部通知标为已读后，右上角未读红色角标没有同步消失的问题；删除未读通知时也会同步更新角标。
- 管理驾驶舱新增“待我确认的人力预约”快捷处理区，被预约人可在首页直接确认或拒绝；待办统计改为仅计算本人待确认记录。
- 优化共享看板时间轴：时间显示为格线刻度点，明确标出结束刻度，并增加独立的午休禁用区，避免将开始时间误解为整段时间。
- 项目首次审批改为普通项目经理提交给本人直属主管；没有有效直属主管时不能提交，项目经理兼具 L3/超级管理员角色时自动通过。
- 新增项目总工时、已预约/剩余工时和追加工时申请；追加额度仍由项目所属部门 L3 审批。
- 新增 HRDB 数据来源边界：正式来源的人员档案、部门和组织只读，本地测试数据仍可维护。
- 没有显式系统角色的新用户和升级历史用户默认获得项目成员；项目、任务和执行数据按本人业务关系收敛。
- 项目仅本项目经理可见和维护，任务仅负责人可见和编辑，执行记录仅记录本人可见和维护；审批待办使用独立接口展示。
- 统一负载容量与工作日历口径：法定节假日不计可用工时、调休工作日计入；同时排除软删除项目、任务和执行记录，并保留项目经理跨项目的本人任务/执行数据。
- 任务状态统一为未开始、进行中、已完成、已暂停、已取消，延期改为动态显示；任务、执行记录和通知采用保留历史的业务软删除。
- 人力预约取消新建草稿步骤，提交后直接待被预约人本人审批；管理员和经理不能代批。
- 新增预约撤回，原提交人可在被预约人确认前撤回并释放项目工时额度。
- 预约统一限制为工作日 `08:30-12:00`、`13:00-17:30`，按 30 分钟自动计算工时且不能跨午休。
- 新增工作日历表、管理 API/页面和 2026 法定节假日/调休数据。
- 系统角色显示名称“部门主管/职能主管”统一更新为 `L3/L4`，角色代码保持兼容。
- 保留历史中文用户名的登录兼容，并修复输入法候选确认时被提前提交、用户名 Unicode 表示或首尾空白不一致的问题。
- 修复用户编辑时清空部门、组织或直属主管后，空值未随请求提交而导致关联无法删除的问题。
- 修改部门时，如果原组织不属于新部门，会自动清除原组织关联，避免残留跨部门数据。
- 新增用户时限制用户名为英文字母、数字和英文符号，姓名仍支持任意语言。
- 新增用户软删除，以及部门、组织、任务、执行记录和个人通知的安全删除能力。
- 增加 `20260911_0003` 数据库迁移，并补充删除前置条件、历史数据保留和错误码说明。

## 2.0.0 - 2026-09-10

- 将首页升级为管理驾驶舱，新增项目健康、风险、完成率、本周工时与 14 日排期趋势。
- 新增人员负载分析，支持日/周/月聚合、利用率、空闲/超负载和项目投入占比。
- 新增风险扫描与处理闭环，覆盖人力冲突、负载超限、任务/项目延期和长期未更新。
- 新增站内通知、通知偏好、临期提醒，以及邮件、企业微信、钉钉投递适配器。
- 新增用户、项目、任务 Excel 模板与逐行导入任务；新增排期、执行、项目过程报表导出。
- 共享看板增加月视图、拖动调整、乐观版本号、多人员批量排期和复制上周。
- 新增计划/实际、延期率、成员达成和项目人力占比经营分析。
- 新增 Redis、Celery Worker/Beat 配置及 V2.0 增量数据库迁移。
- 新增 V2.0 范围、升级、API、数据库和架构说明；明确未执行安装、测试、构建和服务启动。

## 1.0.0 - 2026-09-09

- 建立 Vue 3 + TypeScript + Vite 前端工程。
- 建立 FastAPI + SQLAlchemy + Alembic 后端工程。
- 完成登录认证、用户、组织、RBAC、项目、项目成员、两级任务等基础能力。
- 完成人员共享看板、预约、提交确认、拒绝、重新确认和时间冲突检测。
- 完成任务执行填报、项目过程报表、达成评价、动态延期和人员负载计算。
- 完成操作日志、MySQL 首版迁移、初始化数据脚本和 Docker Compose 部署文件。
- 补充项目、架构、API、数据库和 V1.0 范围文档。
